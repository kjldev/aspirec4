namespace Aspire.Hosting.LikeC4;

/// <summary>
/// Minimal test AppHost used by integration tests.
/// Does not reference any real projects — the test itself adds resources via the builder.
/// </summary>
internal sealed class TestAppHost;
