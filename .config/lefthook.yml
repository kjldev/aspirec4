pre-commit:
  jobs:
    - name: csharpier format check
      run: just lint-check

    - name: biome lint check
      run: just ts-lint

commit-msg:
  jobs:
    - name: conventional commits
      run: npx commitlint --edit {1}
